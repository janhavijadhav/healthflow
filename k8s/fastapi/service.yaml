apiVersion: v1
kind: Service
metadata:
  name: healthflow-api-service
  namespace: healthflow
spec:
  selector:
    app: healthflow-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
